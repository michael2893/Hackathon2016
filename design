import java.util.Scanner;
/**
 * Put a short phrase describing the program here.
 *
 * @author Put your name here
 *
 */
public final class Travel {

    /**
     * Private constructor so this utility class cannot be instantiated.
     */
    private Travel() {
    }

  /*@param budget
    *@param cost
    *@param location
    *@param air
    *@param lodging
    *
    *@returns flight
    *@returns hotel
    *@returns cost
    *@returns location
    *
    *@requires buget
    *@requires preferences
    *
    * @ensures <pre>
    * cost = air + lodging
    * cost < budget
    *  </pre>
    */

    private static void Air(String flight) {
    	
    }

    private static void Lodge(String hotel) {
    	

    }

    private static void Location(String destination) {

    }

    /*@param cost
     *
     *@ensures <pre>
     * cost < budget
     * </pre>
     */

    private static  price(double cash, double cost) {
    	assert cash > cost: "Violation budget exceeded";
    	if(cash <= cost) {
    		Air(flight);
    		Lodge(hotel);
    		Location(destination);
    		
    	}

    }


    /**
     * Main method.
     *
     * @param args
     *            the command line arguments
     */
    public static void main(String[] args) {
    	Scanner in = new Scanner(System.in);
        System.out.println("Enter budget:");



    }

}

